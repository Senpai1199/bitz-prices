import Link from "next/link";
import Layout from "../components/Layout";
import Navbar from "../components/Navbar";

const About = () => (
  <Layout>
    <div>
      <h1>
        This app fetches the current Bitcoin rates in USD, EUR and GBP
        currencies.
      </h1>
    </div>
  </Layout>
);

export default About;
